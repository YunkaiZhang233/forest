\date{2025-07-28T22:08:10Z}
\author{yunkaizhang}
\title{Bicartesian categories are distributive}
\taxon{theorem}

\def\CCat{#{\mathcal{C}}}

\p{Let \CCat be a cartesian closed category in which every pair of objects also has a binary coproduct. Then for all objects #{A}, #{B}, and #{C} in \CCat, #{(Y + Z) \times X \cong (Y \times X) + (Z \times X)}.}